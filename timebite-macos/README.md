# timebite-macos

An Xcode macOS app project.

## Getting Started

1. Open `timebite-macos.xcodeproj` in Xcode.
2. Build and run the `timebite-macos` scheme.

## Notes

- This repository is set up as an Xcode project.
- Keep generated build artifacts and local user settings out of version control.
